[package]
name = "storage"
version = "0.1.0"
authors = ["Alec Von Arx <al.vonarx@gmail.com>"]
edition = "2018"

[dependencies]
# Local repo dependencies
types = { path = "../types" }

# External dependencies

[dev-dependencies]
rand = "=0.7.3"